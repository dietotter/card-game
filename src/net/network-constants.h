#pragma once

namespace nik::cnst {

    inline constexpr int defaultServerPort{ 25565 };
    // id which server will be using to identificate itself
    // (also, serverDefaultId + 1 is the starting id for clients)
    inline constexpr int serverDefaultId{ 0 };
    
}